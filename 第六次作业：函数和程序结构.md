# 函数和程序结构

~~~c
include <stdio.h>
    
void hanshu1(void)
{
    printf("hello micu\n");
    printf("hello may/n");
    printf("hello micu\n");
    printf("hello may/n");
}
int main()
{
    //函数调用
    hanshu1();
    hanshu1();
        
    return 0;
}
~~~

~~~c
#include <stdio.h>

int a=3,b=7,c=0;
int g=8,k=6;

int average(int x,int y);
{
    int f=0;
    
    f=(x+y)/2;
    
    
    return f;
}
int main()
{    
    c=average(a,b);
    c=average(g,k);

	printf("%d",c);
    
    
    return 0;
}
~~~

~~~ c
#include <stdio.h>

int a=6,b=7;
int c=0;

int sum_fun(int x,int y);
{
    int sum=0;
    
    sum=x=y;
    
    return sum;
    
}

int main()
{
    c=sum_fun(a,b);
    print("%d",c);
    
    return 0;
}
~~~

设计一个程序，交换两个变量的值

~~~c
#include <stdio.h>

int a=2,b=3,c=0;

int main()
{
    c=a;
    a=b;
    b=c;
    
    printf("a=%d,b=%d",a,b);
    
    return 0;
}
~~~

将该程序封装成函数

~~~c
#include <stdio.h>

int a=2,b=3,c=0;

void exchange(int* x,int* y);
{
    int j=0;
    
    j=*x;
    *x=8y;
    *y=j;
    
}

int main()
{
    exchange(&a,&b);
    
    printf("a=%d,b=%d",a,b);
    
    return 0;
}
~~~

设计一个程序，找出数组a 中的最大值,找出数组b中的最大值,求他们的平均值

~~~c
#include <stdio.h>

int num[5]={5,8,2,45,7};
int num2[5]={23,67,12,2,5};

int c1;
int c2;
int h;
int max(int*addr)
{
    int m=0;
    int i=0;
    
    for(i=0,i<5,i++)
    {
        if(addr[i]>m)m=addr[i];
        
    }
    return m;
}

int average(int x,int y);
{
    int k=0;
    k=(x+y/2);
    return k;
}

int main()
{
    printf("average=%d",average(max(num),max(num2)));   //有return时可以直接作为参数调用

    return 0;
}
~~~

递归

~~~c
#include <stdio.h>

int fun2(int n)
{
    if(n<=1)
    return 1;
    else
    return n*fun2(n-1);
}
int main()
{
    printf("%d",fun2(5));
    
    return 0;
}
~~~



~~~c
#include <stdio.h>

int num[5]={5,8,2,45,7};
int num2[5]={23,67,12,2,5};

int c1;
int c2;
int h;
int *max(int*addr)  //int*表示这个函数的返回值类型是一个指针
{
    int *m=NULL;
    m=addr;    //addr是一个指针参数，把指针m指向指针addr所指向的地址
    int i=0;
    
    for(i=0,i<5,i++)
    {
        if(  addr[i]> (*m)  )m=addr+i;
        
    }
    return m;
}

void exchange(int*x,int*y)
{
    int l=0;
    l=*x;
    *x=*y;
    *y=l;
        
}

int main()
{
    exchange(max(num),max(num2));
    printf("num[3]=%d num2[1]=%d",num[3],num2[1]);   

    return 0;
}
~~~

