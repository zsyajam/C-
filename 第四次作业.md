# 数组和指针入门                                                                                                                                                                                                         

## 一堆数组

1.假设已经存储了3名学生的成绩信息，学号为1，2，3.试写一C代码让用户输入学号查询对应学员成绩

~~~ C
# include <stdio.h.>

int student1=45;
int student2=56;
int student3=67;
int num;

int main()
{
    printf("请输入学员的学号")
    scanf("%d",&num);
    switch(num)
    {
    	case 1:
        	printf("学员1的成绩为:%d",student1)
        	break;
    	case 2:
        	printf("学员2的成绩为:%d",student2)
        	break; 
    	case 3:
             printf("学员3的成绩为:%d",student3)
    		break;
            
    }
    
return 0;
}
~~~

2.假设需要顺序输入3名学生的成绩信息，学号为1，2，3，输入完成后支持学号查询成绩信息

~~~ c
# include <stdio.h.>

int student1=45;
int student2=56;
int student3=67;
int num;

int main()
{
    printf("请输入学员1的成绩");
    scanf("%d",&num);
    
    printf("请输入学员2的学号")
    scanf("%d",&num);
    
    printf("请输入学员3的学号")
    scanf("%d",&num);
    
    switch(num)
    {
    	case 1:
        	printf("学员1的成绩为:%d",student1)
        	break;
    	case 2:
        	printf("学员2的成绩为:%d",student2)
        	break; 
    	case 3:
             printf("学员3的成绩为:%d",student3)
    		break;
            
    }
    
return 0;
}
~~~

3.假设需要乱序输入10名学生的成绩信息，假设需要输入不定量学生的成绩信息......
 ***数组的格式：数组名+[数组下标]***

~~~c
# include <stdio.h.>

int student[3]={0};          0  1  2
int i;
int j;

int main()
{
    for(i=0;i<3;i++)
    {
        printf("请输入学员的学号");
        scanf("%d",&j);
        printf("请输入学员的成绩");
        scanf("%d",&student[j-1];
              
    printf("请输入学员的学号")；
    scanf("%d",&j);
              
    printf("学员%d的成绩为：%d,j,dtudent[j-1]")
    
       return 0; 
    }
~~~

4.思考题：

用户输入10个数，将这10个数按从小到大的顺序排序。

~~~ C
#include <stdio.h>

int main()
{
	int arr[10]; //数组，存10个数字
    int i,j,temp;
    
    //第一步输入去哦个数字
    printf("请输入10个整数：\n");
    for(i=0;i<10;i++)
    {
        scanf("%d",&arr[i]);
    // 冒泡排序
    	for(i=0;i< 9;i++)
   	 	{
        	if(arr[j]>arr[j+1])
        	{
            	temp = arr[j];
            	arr[j] = arr[j+1];
            	arr[j+1]=temp;
        	}
    	}
    }
    printf("从小到大排序后的结果是:\n")
    for(i=0;i<10;i++)
    {
        printf("%d",arr[i]);
    }
    printf("\n")
        
    return 0;
}
~~~

## 多维数组

int num[5][9]

~~~ [
#include <stdio.h>

int a[2][3]={   {1,2,3},
                {4,5,6}
                
};
int main()
{

	printf("%d",a[0][2]);
	
	return 0;
}
~~~

### 常量数组

const 关键字

const char a[]={.........}

字符，单引号

char string[3]={‘a’,’b’,’c’};

char string[]=“abc”;

printf(“%d”,string[2]);

## 指针

每个指针都指向唯一的字节

指针变量：存储地址的变量

int a;int b;float c;

int*a_p;int*b_p;int*c_p;

a=1;

&a=3;取地址

a_p=3;

指针变量的使用：

*a_p=1;
